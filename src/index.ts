class Streak {
    startDate: Date;
    endDate: Date;
    progress: number;
  
    constructor(startDate: Date) {
      this.startDate = startDate;
      this.endDate = Date;
      this.progress = 0;
    }
  
    updateProgress() {
      // Logic for updating progress
    }
  
    calculateStreakLength() {
      // Logic for calculating streak length
    }
  }



  interface StreakData {
    id: number;
    startDate: string;
    endDate: string;
    progress: number;
  }
  